# Given an array of integers, remove all the duplicates and return a new array without duplicates
# The order of elements should be preserved

arr = [2, 3, 5, 3, 2, 6, 7, 8, 7, 5]
unique = list(set(arr))
print("Array without duplicates:", unique)
print("\n")